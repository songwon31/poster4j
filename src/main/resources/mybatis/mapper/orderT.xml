<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.webteam2.poster4j.dao.OrderTDao">
	<select id="selectOrderTById" parameterType="int" resultType="OrderT">
		select order_id, customer_id, receiver_id, coupon_id, order_date, order_settlement_method
		from order_t
		where order_id = #{orderId}
	</select> 
	
	<select id="count" resultType="int">
		select count(*) from order
	</select>
	
	<select id="insert" parameterType="OrderT">
		insert into order_t (order_id, customer_id, receiver_id, coupon_id, order_date, order_settlement_method)
		values (order_t_seq.nextval, #{customerId}, #{receiverId}, #{couponId, jdbcType=DECIMAL }, #{orderDate}, #{orderSettlementMethod})
	</select>
	
	<select id="selectOrderListById" parameterType="int" resultType="OrderT">
		select *
		from order_t
		where order_id = #{orderId}
	</select>
</mapper>