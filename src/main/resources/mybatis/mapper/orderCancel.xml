<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.webteam2.poster4j.dao.OrderCancelDao">
	<insert id="insertOrderCancel" parameterType="OrderCancel">
		<!-- null이 들어갈 수 있는 컬럼값은 반드시 jdbcType을 명시해야 함 -->
		insert into order_cancel (order_id, product_id, order_cancel_category, order_cancel_reason, 
							 order_cancel_req_date)
		values(#{orderId}, #{productId}, #{orderCancelCategory},
			   #{orderCancelReason, jdbcType=VARCHAR}, #{orderCancelReqDate})
	</insert>
</mapper>