<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.webteam2.poster4j.dao.CustomerDao">
	<select id="selectById" parameterType="String" resultType="Customer">
		select customer_id, customer_name, customer_telno
		from customer
		where customer_id = #{customerId}
	</select>
	<insert id="insert" parameterType="Customer">
		insert into customer (customer_id, customer_password, customer_name, customer_telno, customer_email, customer_term_agree, customer_info_agree, customer_sms_agree, customer_email_agree)
		values(#{customerId}, #{customerPassword}, #{customerName}, #{customerTelNo}, #{customerEmail}, #{customerTermAgree}, #{customerInfoAgree}, #{customerSmsAgree}, #{customerEmailAgree})
	</insert>
</mapper>

